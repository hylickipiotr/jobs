import React from "react";
import Layout from "../components/Layout/Layout";

const Saved: React.FC = () => {
  return <Layout title="Jobs - Zapisane oferty">Saved</Layout>;
};

export default Saved;
